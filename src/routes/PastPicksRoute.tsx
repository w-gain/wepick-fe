import { RoutePlaceholder } from './RoutePlaceholder';

export function Component() {
  return (
    <RoutePlaceholder
      screenId="SCR-002"
      title="지난 Pick"
      description="지난 Pick 목록은 후속 화면 구현 단계에서 추가합니다."
    />
  );
}
